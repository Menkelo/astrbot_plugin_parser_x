# Changelog

本项目遵循语义化版本，版本更新同时记录用户可见变化、兼容迁移和验证范围。

## [0.5.16] - 2026-08-20

### Fixed

- 统一调试页的错误信息结构：解析、投递、会话、连接和媒体错误均明确显示错误标题、
  所属平台/阶段、具体原因与建议操作，不再混用系统提示、原始异常和模糊 Toast 文案。
- 完整失败不再显示绿色“解析完成”；全部失败显示红色失败汇总，部分成功显示黄色
  “完成但有问题”汇总，并保留每个平台各自的问题卡片。
- 未知异常只在页面提示日志位置，不再直接暴露内部异常内容；取消任务使用独立信息状态，
  避免重复显示或被当作失败。
- 兼容 AstrBot Plugin Page bridge 仅转发错误字符串的行为，仍能在页面还原标题、原因
  和处理建议。

### Validation

- 自动化回归测试共 126 项通过；Ruff、JavaScript 语法和 Git 差异检查通过。
- 浏览器覆盖完整失败、部分成功、启动失败和状态接口失败，均无控制台错误或横向溢出。

## [0.5.15] - 2026-08-20

### Changed

- 将插件调试页收敛为单一 PC QQ 对话窗口，移除独立控制侧栏、平台清单和原始解析数据区；
  分享文本直接从窗口底部输入，真实解析结果按左右消息气泡、引用、媒体卡片、文件和合并
  转发结构显示。
- 重新区分消息正文与图片、视频、音频的文件信息栏，媒体名称、大小与保存操作不再混入
  正文气泡；合并转发默认折叠并提供消息摘要。
- 调整桌面与窄屏布局，保留调试模式状态、解析进度、取消和清空操作，同时防止状态查询
  阶段重复发送以及运行中清空导致的会话回填。

### Validation

- 自动化回归、JavaScript 语法、Ruff 与浏览器视觉检查通过；覆盖初始窗口、完整模拟消息流
  和 520px 窄屏无横向溢出。

## [0.5.14] - 2026-08-15

### Added

- 米游社解析优先读取 `structured_content` 富文本顺序，正文与内嵌图片可按原文位置交替
  还原，并保留折叠内容与不可用时的旧字段回退。
- 调试页增加 PC QQ 风格的聊天消息预览，区分自己发送、Parser X 回复、引用、媒体与
  合并转发结构。

### Removed

- 完整移除西瓜视频、微视和皮皮虾的平台路由、配置开关、主题与兼容文档；旧配置中的
  对应开关会在升级时清理，其他 yt-dlp 用途不受影响。

### Validation

- 自动化回归测试共 124 项通过。

## [0.5.13] - 2026-08-15

### Fixed

- 修复米游社与小黑盒图文作品被拆成多个合并转发节点的问题；现在正文、配图和平台
  个性表情会按原顺序渲染为一张图片并直接发送。
- 含视频作品继续让一图流、源视频和评论区并发生成与发送；一图流渲染失败时才回退到
  原生文本与图片节点，避免内容丢失，同时不会重复发送视频。

### Validation

- 自动化回归测试共 125 项通过，覆盖两平台纯图文一图流、视频独立发送、失败回退及
  其他平台既有投递规则。

## [0.5.12] - 2026-08-15

### Added

- 新增 AstrBot 官方 Plugin Page「解析调试台」：直接复用当前插件实例、平台开关、Cookie、
  解析器、下载器、Canvas 评论渲染和真实消息投递逻辑，无需再到 QQ 反复发送链接测试。
- 调试结果通过 SSE 按真实发送顺序展示引用、文本、图片、视频、音频、文件及合并转发节点；
  本地媒体使用短期不透明令牌预览或下载，并按 Dashboard 用户隔离。
- 插件配置新增默认关闭的 `debug.enabled` 独占调试开关。开启后 QQ 等消息适配器不再触发
  Parser X，只有已登录 Dashboard 中的插件调试页可以启动解析任务。

### Fixed

- 修复米游社和小黑盒内容含视频时被通用视频分支提前截断、原生正文与配图批次未发送的问题；
  现在会保留平台原生图文投递，并仅在视频作品中并发生成评论区。
- 修复调试会话在任务刚创建、协程尚未开始时立即取消可能缺少结束事件的问题；会话和媒体
  令牌均校验所属 Dashboard 用户，过期会话会完成清理。

### Changed

- 完成“无正文卡”发送规则迁移：普通视频只发送源视频，单图引用原消息，多图按一图一节点
  合并转发，纯文本内容才发送正文；评论区仅随视频作品发送。米游社与小黑盒继续使用原生
  图文兼并批次，不套用图文分离平台规则。

### Validation

- 自动化回归测试共 123 项通过，覆盖调试开关、Page 鉴权、路由去重、真实投递捕获、SSE
  完成/取消、媒体令牌隔离、合并转发节点结构及各平台无正文卡发送规则。

## [0.5.11] - 2026-08-15

### Fixed

- 小黑盒正文已进入统一长卡后不再重复发送帖子概要、富文本正文和原图转发；长卡渲染或发送
  失败时仍会恢复原生图文批次，避免正文或图片丢失。

### Changed

- B站图文动态、抖音图文/图集、小红书图文和无视频微博改为原图优先：单图直接引用原消息，
  多图逐张放入合并转发；正文卡不再使用作品图片作为封面。
- 上述图文分离作品的正文卡改为附属转发节点，评论长图可用时会并入同一条合并转发，每张
  原图、正文卡和评论图仍各占一个节点；卡片失败时使用正文文本节点回退。
- 米游社文章与小黑盒帖子改为图文一体长卡：正文、平台表情和全部正文配图进入同一张卡，
  成功后不再另外发送原正文/原图；视频仍独立并发发送，评论仍使用独立合并转发。

### Validation

- 自动化回归测试扩展到 113 项，覆盖分离型平台单图直引、多图/正文卡/评论图合并转发、
  无封面正文卡、小黑盒与米游社有序图文流、成功去重及卡片失败回退。

## [0.5.10] - 2026-08-15

### Fixed

- 正文卡不再等待评论抓取，也不再把热门评论拼进正文形成超长图；评论抓取、头像处理或渲染
  失败不会拖住正文卡和视频。
- 小黑盒恢复独立评论图后仍会先加载官方表情目录和头像，避免退回离线兜底导致个性表情失效。

### Changed

- 视频作品的正文卡与视频文件改为并发准备并各自完成即发送，不再固定“先长卡、后视频”；
  普通解析结果和微博/米游社/小黑盒等投递计划均使用同一完成顺序逻辑。
- B站、抖音、微博、米游社和小黑盒恢复独立评论区图片，并统一放入一条合并转发：首节点为
  平台热门评论标题，后续每张评论图各占一个节点；转发失败时降级为标题和图片逐条发送。

### Validation

- 自动化回归测试扩展到 108 项，覆盖正文卡/视频双向完成顺序、投递计划并发顺序、正文卡不再
  接收评论文档、评论图合并转发、评论失败隔离和转发失败降级。

## [0.5.9] - 2026-08-15

### Fixed

- B站单图动态不再绕过统一长卡；动态正文、作者和单图会共同进入引用原消息的内容卡。
- 小红书等无标题、无正文的纯图片作品不再把带 `xsec_token` 的分享链接填进正文区域。
- 审计正常作品的长卡开关后，为网易云音乐补齐统一长卡；B站直播和明确的错误提示仍保留
  原生消息形态。

### Changed

- 单图作品统一只在长卡主视觉中出现一次，不再于卡片后重复发送同一张原图；卡片生成或
  引用失败时仍会回退发送正文和原图。多图继续按一张图片一个转发节点发送。
- 单图主视觉默认使用 `contain` 并提高可用高度，避免取消原图消息后出现裁切；同一图片不再
  同时充当“封面”和后续原图。
- 移除顶栏的背景模糊、高光、颗粒和内阴影，固定平台代表色改为简洁纯色顶栏。

### Validation

- 自动化回归测试扩展到 103 项，覆盖 B站单图动态长卡、单图媒体去重、投递计划去重、
  小红书空正文以及网易云音乐统一长卡。

## [0.5.8] - 2026-08-15

### Fixed

- 完成其余平台表情路径审计：B站正文会读取公共表情目录、已购表情包和动态富文本自带的
  自定义表情地址；微博正文 HTML 表情会进入统一长卡；抖音作品元数据表情和更多贴纸字段
  也会正确渲染。原有 B站/抖音/微博评论表情路径继续保留。
- 小红书正文中的 `#话题名[话题]#` 内部标记会清理为标准 `#话题名#`，普通正文中的
  `[话题]` 文本不会被误删。

### Changed

- 统一长卡顶栏不再从作品图片取色，固定使用各平台代表色，并叠加半透明高光、细颗粒、
  内描边与背景模糊，形成稳定的磨砂玻璃质感。
- AcFun、西瓜视频、皮皮虾和微视等兼容层补齐各自的固定代表色，不再统一回退灰色顶栏。

### Validation

- 自动化回归测试扩展到 101 项，覆盖 B站公共/已购/动态自定义表情、微博正文表情、抖音
  备用贴纸字段、小红书话题清洗、固定平台色与磨砂玻璃顶栏。
- 在线核验 B站官方表情目录可解析 `[doge]`、`[笑哭]`、`[原神_欸嘿]`，并使用真实 Chromium
  复核小红书固定红色磨砂顶栏及卡片无水平溢出。

## [0.5.7] - 2026-08-15

### Fixed

- 米游社与小黑盒正文卡、热门评论现在会读取平台官方表情目录并渲染真实表情图片；米游社
  评论中的用户自定义表情会按贴纸尺寸展示。目录临时不可用时保留常用表情离线兜底。
- 小黑盒富文本不再把 Android 的 `optimizer_output`、本地缓存路径或其他客户端内部文件字段
  当作正文；帖子完整正文也会进入统一长卡，不再只显示简介。
- 小红书视频补齐统一长卡；审计后同步补齐抖音、快手以及 AcFun/西瓜视频/皮皮虾/微视等
  yt-dlp 视频兼容层的长卡，视频文件继续作为后续独立消息发送。
- 合并转发改为“一段内容一个节点”：多段文字和多张图片分别显示为多条转发记录，不再把
  多张图片或文字与图片粘进同一个节点。

### Changed

- 小黑盒富文本中的官方表情会在统一长卡内行内显示，并在原始富文本转发中拆成独立表情
  图片节点；同一表情重复出现时复用下载结果，未命中官方目录的普通方括号正文保持原样。

### Validation

- 自动化回归测试扩展到 94 项，新增小黑盒 Android 缓存路径、米游社自定义表情、两平台
  官方表情、原生/兼容层视频长卡，以及每个合并转发节点仅含一个消息段的覆盖。
- `pytest`、Ruff、格式检查、`compileall` 和 `git diff --check` 全部通过。

## [0.5.6] - 2026-08-15

### Changed

- 统一长图改为一条独立的引用消息：先引用触发解析的原消息发送正文长卡，再按平台原有批次
  发送原图、富文本合并转发和视频，不再把长卡塞进图片或转发节点。
- 长卡顶栏优先从作品图片提取主色并自动压暗到白字可读范围；没有可用图片时回退平台品牌色。
  左上角不再显示 `B`、`米`、`微` 等字母或单字平台标记。
- 热门评论只保留统一长卡内嵌路径；抓取、片段生成或合并失败时发送无评论正文卡，不再后台
  生成旧式独立评论长图。

### Removed

- 移除已经被统一正文卡取代的 B站专用动态卡渲染器、独立评论任务工厂和后台评论图发送器。
- 移除不再生效的“评论图 JPEG 质量”配置项；统一正文卡和内嵌评论继续使用 PNG。

### Validation

- 自动化回归测试扩展到 91 项，覆盖动态取色/对比度、无平台单字标记、长卡独立引用、原图与
  富文本/视频分批发送、评论合并失败不回退旧评论图，以及五个平台只暴露结构化评论工厂。
- 在 760px 真实浏览器视口复核统一长卡：左右边距均为 22px、无水平溢出、顶栏使用媒体主色、
  评论和楼中楼未越界。

## [0.5.5] - 2026-08-15

### Changed

- 微博、米游社和小黑盒不再把正文长卡与后续图文拆成多条消息：连续的文字/图片批次会与
  正文卡合并为同一个直接消息或合并转发，视频仍保持独立发送。
- 抖音、快手、小红书和 B站多图作品的正文卡与原图改为放进同一个合并转发节点，避免在
  转发记录中被拆成多个独立节点。

### Fixed

- 修复 B站单图动态只引用原图、没有附带正文的问题；现在同一条引用消息会包含动态信息、
  完整正文和原图。
- B站转发动态会补回原动态正文、作者和图片；详情接口没有正文时自动回退 opus 页面提取，
  避免新版动态字段或转发动态显示“无正文”。

### Validation

- 自动化回归测试扩展到 88 项，覆盖 B站单图正文、转发动态正文/图片、微博单图合并、
  小黑盒富文本批次合并、多图单节点及视频独立发送。
- `pytest`、Ruff、格式检查、`compileall` 和 `git diff --check` 全部通过。

## [0.5.4] - 2026-08-15

### Added

- 新增“正文 + 热门评论”纵向长卡：平台色顶栏、主视觉、标题/时间/真实互动数据、作者、
  完整正文、作品信息、热门评论和仓库页脚按固定顺序排列。
- B站视频加入统一长卡，展示封面、UP 主头像、播放/评论/点赞/收藏/投币/分享、时长、
  分 P 和分区；视频文件仍作为独立消息发送。
- B站、抖音、微博、小黑盒和米游社评论抓取新增结构化文档路径，可直接嵌入正文长卡；
  原独立评论长图保留为降级方案。

### Changed

- B站多图/纯文字动态、抖音/快手/小红书图文、微博、米游社和小黑盒统一复用长卡骨架；
  B站单图动态继续只引用原消息，平台原有媒体批次与小黑盒富文本顺序不变。
- 微博纯文字作品不再把头像放大成主视觉；长微博和转发原文继续完整进入正文卡。
- 评论合并最多等待 15 秒；结构化抓取、评论片段或整卡渲染失败时，先生成无评论正文卡，
  再按旧路径尝试独立评论卡，评论故障不会阻断正文。

### Fixed

- 修复 760px 视口中纵向滚动条占宽导致正文、楼中楼和评论配图右侧被裁切的问题；正文卡
  和两套评论卡均隐藏滚动条占位，左右固定保留 22px 对称边距。
- 修复结构化评论已抓取但合成长卡失败时被直接丢弃的问题。
- 小红书发布时间同时兼容秒级和毫秒级时间戳，避免错误显示到 1970 年。
- 评论头像继续使用圆形裁切和首字兜底；回复操作使用真实气泡 SVG，不再出现虚线圈字形。

### Validation

- 自动化回归测试扩展到 84 项，覆盖长卡层级、微博正文、评论成功/失败降级、B站视频、
  单图动态、视频独立发送、小红书时间戳和各平台结构化评论工厂。
- 使用真实浏览器在 760px 视口逐一复核 B站、抖音、快手、米游社、微博、小黑盒和小红书：
  七个平台均为 22px 对称边距、无水平溢出，长正文、评论配图和楼中楼未越界。

## [0.5.3] - 2026-08-15

### Changed

- 评论区统一采用“极简信息流”骨架：B站、抖音、微博、小黑盒和米游社共享圆形平台标记、
  中性分隔线、楼中楼缩进、互动图标和双列页脚；平台差异仅保留品牌色，B站等级、粉丝牌、
  UP 标识等语义字段继续完整展示。
- B站动态、米游社、小黑盒、小红书图文和微博正文卡统一使用同一内容层级与间距；微博、
  米游社和小黑盒仍按原生消息批次发送媒体，单图引用、富文本顺序和视频独立发送不变。
- B站单图动态继续跳过概览卡并直接引用原消息；其余已启用正文卡的平台在官方渲染失败时
  自动保留原纯文本概览，避免正文再次丢失。

### Fixed

- 回复和点赞操作改用真实描边 SVG，不再依赖可能显示成虚线圈或异形字形的字体符号。
- 评论长图继续按 1140px Canvas 宽度缩放和裁切，卡片左右留白保持对称；头像统一使用圆形
  `object-fit: cover`，加载失败时显示作者首字占位。

### Validation

- 新增五个平台内容主题、投递计划正文卡替换及渲染失败回退测试；完整路由测试 72 项通过。
- 使用真实浏览器复核 760px 内容卡与 1140px 评论长图，无水平溢出，平台页脚和楼中楼可见。

## [0.5.2] - 2026-08-15

### Added

- 新增小黑盒帖子评论区，复用帖子接口返回的评论线程，支持头像、作者标识、等级、IP 属地、
  点赞数、评论配图和首条楼中楼。
- 新增米游社热门评论区，使用公开 `getPostReplies` 接口，支持认证、等级、IP 属地、配图和
  首条楼中楼；两个平台均复用 AstrBot 官方 `html_render`。

### Changed

- 微博、小黑盒和米游社不再生成正文文字卡，改用原生消息链：概览/正文先发、图片按平台批次
  发送、视频始终单独发送。单图批次直接引用原消息，多图转发失败时按原顺序逐段降级。
- B站直播移除专用 HTML 卡片，改为封面、关键帧与直播信息原生消息链；插件运行时不再保留
  任何直播卡片渲染入口。
- 小黑盒无论是否配置 Cookie 都会先尝试签名接口；失败后才回退公开游戏接口或官方分享页，
  避免公开帖子被直接降级而出现无响应。
- 小黑盒富文本正文按原始图文顺序发送；米游社 HTML 正文会先转换为纯文本。

### Fixed

- 修复微博单图消息把正文和引用图片塞进同一消息链、部分协议端只显示图片的问题；正文现在
  始终作为第一条纯文本发送，长微博缺少内嵌全文时会补请求完整正文。
- 修复微博主内容请求未携带已配置 Cookie，以及公开 API 返回空正文时没有回退详情页的问题；
  转发微博会同时保留原博正文，发送前还会校验投递计划并自动补回缺失正文。
- 修复评论区回复 SVG 使用描边样式渲染实心路径而显示成虚线圈的问题，并提升缓存版本，强制
  重新生成微博头像与新版回复图标。

### Validation

- 覆盖正文必发、单图引用、多图批次、视频分开发送、合并转发降级、评论适配和无 Cookie
  小黑盒 API 优先级的自动化回归测试。
- 运行时浏览器审计扩展到全部 `main.py` 与 `core/**/*.py`，确认不存在插件自身的
  Playwright/Chromium 启动路径。

## [0.5.1] - 2026-08-15

### Changed

- 微博、小黑盒、米游社以及抖音/快手/小红书图文作品的正文统一使用共享正文卡渲染；
  单图作品仍直接引用原消息发送“正文卡 + 原图”，不强制改成合并转发。
- 小黑盒帖子正文改为读取 `link.text` 富文本并提取内嵌图片；旧 App 链接会改试官方分享地址，
  新版 `community/.../list/...`、`games/detail/...` 链接也可识别，游戏卡在未配置 Cookie 时
  优先使用公开游戏详情接口。

### Fixed

- 修复微博含图片或视频时正文未进入发送链路的问题；正文卡渲染失败时回退纯文本，避免再次吞正文。
- 补齐快手和抖音原生结果中的规范原链接，避免下载/发送失败提示缺少回跳地址。

### Removed

- 移除贴吧解析器、平台开关和外部详情 API 配置。贴吧官方页面目前只稳定返回小程序壳，
  楼主正文依赖非官方社区服务，无法作为可靠支持继续保留；升级时会清理旧贴吧配置。

## [0.5.0] - 2026-08-14

### Changed

- 新增共享 `HtmlRenderService`，正文卡、B站动态、B站直播以及 B站/抖音/微博评论卡统一复用
  AstrBot 官方 `Star.html_render()`（Canvas/T2I）。
- 移除插件内所有 Playwright / 本地 Chromium 启动路径和运行时依赖；官方渲染失败时由调用方
  按原有容错策略跳过卡片或回退文本，不再维护第二套浏览器环境。
- 保留官方截图结果的轻量裁边，避免 AstrBot 默认画布在卡片右侧和底部留下空白。
- 按 AstrBot 最新配置 Schema 重组配置面板：新增“卡片渲染”和“发送行为”分组，使用
  `obvious_hint`、下拉 `labels`、滑块、折叠项和 `file` 上传组件。
- 自动迁移旧版顶层 `show_download_fail_tip`、`disabled_sessions` 和 `bili_comment` 配置，
  保留升级前行为。

### Fixed

- 微博主评论及楼中楼头像改为带微博 Referer 下载后内嵌，避免新浪 CDN 防盗链导致字母占位。
- 小黑盒支持 `api.xiaoheihe.cn` 等分享子域名，并读取分享跳转中的帖子标题与简介。
- 评论区页脚统一为 `Menkelo/astrbot_plugin_parser_x`。

### Validation

- 路由、配置迁移、评论适配、官方渲染调用与发送降级均有自动化回归测试。
- 提交前执行 `pytest`、Ruff、格式检查和 Python 编译检查。

## [0.4.0] - 2026-08-12

### Added

- 建立面向 AstrBot OneBot v11 的国内平台解析基线。
- 原生支持 Bilibili、抖音、快手、微博、小红书、米游社、贴吧、小黑盒，并加入有限的
  yt-dlp 国内平台兼容层。
- 建立上游兼容清单、同步流程和定时漂移检查。
