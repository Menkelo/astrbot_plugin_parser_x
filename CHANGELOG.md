# [v1.3.6] - 动态图策略回退

* B 站 opus / 动态相册回退为发送静态图，不再读取 `live_url` 作为动态图视频。
* 小红书图文笔记回退到静态图发送逻辑，移除 live photo 视频与音频混合路径。
* 抖音图文和 slides 回退为静态图/封面发送，不再尝试发送 `images[].video` 动图或静图+音频合成视频。
* 删除已不再使用的抖音媒体合成器代码，减少后续维护负担。

---

# [v1.3.5] - 小红书静图误判修复

* 收窄小红书动态图视频 URL 判断，不再把普通 `xhscdn.com` 静态图链接当作 live photo 视频。
* 保留 `sns-video`、`.mp4`、`livePhotoVideoKey` 等明确动态图视频特征识别。

---

# [v1.3.4] - Live Photo 媒体修复

* B 站 opus / 动态支持读取相册图片中的 `live_url`，动态图不再按静态图发送。
* B 站动态接口触发风控时，网页 `window.__INITIAL_STATE__` 兜底解析改为括号配平提取，兼容同一 script 内追加初始化代码的页面。
* 抖音 live 图文在只暴露静态封面和音频时，会自动合成为短视频发送，并避免把 mp3 播放地址误判成视频。
* 小红书 live photo 视频会尝试提取笔记音频，原视频无音轨时自动混入背景音。

---

# [v1.3.3] - 小红书动态图支持

* 小红书 normal 图文笔记支持逐张识别 live photo / 动态图视频资源。
* 图文顺序保持不变：单张图带动态图视频时发送视频，否则发送静态图片，避免重复发封面。
* 动态图视频提取兼容 `livePhoto` / `video` / `media` / stream / videoKey 等多种字段形态。
* 抖音动态图逻辑已存在，本版保持原有静图 + 动图混合解析与 BGM 合成逻辑不变。

---

# [v1.3.2] - B站动态解析增强

* B站 b23 / bili2233 短链优先使用 HEAD 跟随跳转，减少短链解析时的页面正文请求。
* B站动态 / opus 正文提取补齐富文本节点，保留话题、网页链接、公式等可见内容。
* B站 opus 模块列表支持提取标题、话题、引用、列表、链接卡、顶部相册图和段落图片。
* 保持现有动态卡片渲染入口不变，继续复用统一图文卡片样式与浅蓝高亮。

---

# [v1.3.1] - 缓存清理日志瘦身

* 缓存清理默认改为每天 00:00 执行，保留最近 24 小时缓存。
* 移除 CacheCleaner 正常启动与清理完成的后台 info 日志，只保留异常日志。
* 收敛图片 data URI 相关工具函数，减少重复代码。

---

# [v1.3.0] - B站空间 / 直播卡片化与分层重构

* **🧩 Bilibili Space 主页卡片**
  * 新增空间链接识别与解析：
    * `space.bilibili.com/{mid}`
    * `m.bilibili.com/space/{mid}`
  * 生成 UP 主空间信息图（头像、昵称、签名、关注/粉丝/稿件、代表作）
  * 代表作策略优化：优先代表作位，失败时回退播放量兜底路径
  * 代表作空缺时支持隐藏作品模块，避免占位卡片影响观感

* **📡 Bilibili Live 直播卡片**
  * 新增直播链接识别与解析：
    * `live.bilibili.com/{room_id}`
  * 生成直播信息卡片（封面、标题、主播、分区、人气、状态）
  * 直播详情请求增加多链路降级与异常兜底
  * 渲染样式优化：封面比例自适应，避免裁切失真
  * 卡片增加版权信息展示（左下角）

* **🏗️ 结构分层重构**
  * B站解析进一步分层：
    * `space_service.py`（空间解析）
    * `live_service.py`（直播解析）
    * `space_renderer.py`（空间卡片渲染）
    * `live_renderer.py`（直播卡片渲染）
  * `bilibili/__init__.py` 职责收敛为路由与编排

* **🖼️ 渲染体验优化**
  * 评论区图片渲染调整为完整显示（避免 `cover` 裁切）
  * 空间卡片与直播卡片封面比例适配增强（竖图 / 超宽图）
  * 版权信息位置与视觉细节优化

* **🛠️ 稳定性与可观测性**
  * 增加链路日志可观测性（接口 code/message）
  * 频控场景增强退避与兜底策略
  * 缓存命中与回退路径优化，减少空卡输出

---
<details>
<summary>📋 点击查看历史更新日志</summary>

# [v1.2.0] - B站 / 抖音解析能力强化与结构优化

* **🚀 Bilibili 解析提速**
  * 新增视频信息与播放地址轻量缓存（`get_info / get_download_url`）
  * 减少重复请求带来的等待时间，重复解析同链接响应更快
  * 评论卡片渲染改为异步任务，避免阻塞主视频主链路

* **🧹 Bilibili 评论区质量过滤（双层）**
  * 第一层：文本规则过滤（引流词、联系方式、短链等）
  * 第二层：二维码图片识别过滤（依赖 OpenCV + numpy）
  * 支持二维码检测次数上限与超时控制，兼顾效果与性能

* **🧩 Bilibili 结构分层**
  * 拆分评论相关能力至独立模块：
    * `comment_service.py`
    * `comment_renderer.py`
    * `stream_selector.py`
  * 主解析器 `__init__.py` 聚焦路由与流程编排，可维护性提升

* **🎬 Douyin 多段动态视频解析增强**
  * 支持短链场景下的多段动态图（`images[].video`）提取
  * 修复仅识别单条媒体的问题
  * 动态视频去重逻辑增强（`uri / video_id / fallback key`）

* **📦 Douyin 下载稳定性修复**
  * 修复动态视频文件同名覆盖（如 `play.mp4`）导致丢失/重复的问题
  * 为动态视频分段下载引入唯一命名策略
  * 图集含动态视频时可按策略优先发送视频内容

* **🎞️ Douyin 合并能力**
  * 支持多段动态图自动合并为单视频
  * 支持提取背景音乐并尝试合成到合并后视频
  * 兼容无音乐场景自动回退无音轨合并结果

* **🛠️ 下载兜底与健壮性**
  * 下载链路保持多级兜底：`curl_cffi -> aiohttp -> yt-dlp`
  * 强化异常处理与日志可观测性，便于排障

# [v1.1.0] - 多平台解析基础优化

* **🔗 链接识别增强**
  * 增强短链与多种 URL 结构匹配能力
  * 提升消息中混合文本场景下的提取成功率

* **📥 下载链路优化**
  * 增加并发下载控制与大小限制约束
  * 优化缓存命中逻辑，减少重复下载

* **🧱 基础能力完善**
  * 统一异常模型与发送流程
  * 改善媒体发送失败时的降级提示

# [v1.0.0] - 初始版本

* **🎉 发布**: 插件初始版本发布
* **✨ 功能列表**:
  * 多平台链接自动识别与解析
  * 视频 / 图片 / 图集基础下载与发送
  * 基础配置与会话开关控制

</details>
