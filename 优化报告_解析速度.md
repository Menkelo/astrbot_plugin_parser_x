# R-Parser 解析速度优化报告

> 范围：**只分析、不改代码**。重点放在**下载/合并速度**与 **B站 / 抖音**两个平台的解析链路。
> 每条都标注了：影响、风险、预计收益。按收益从高到低排列。

---

## 一、结论速览（按优先级）

| # | 优化点 | 所在文件 | 类型 | 收益 | 风险 |
|---|--------|----------|------|------|------|
| 1 | 大视频改用**分段并发下载（HTTP Range）** | `core/download.py` | 下载吞吐 | ⭐⭐⭐⭐⭐ | 中 |
| 2 | 抖音图集**每张图都新建 curl session** → 复用 | `core/download.py` `_download_douyin_image` | 下载延迟 | ⭐⭐⭐⭐ | 低 |
| 3 | 抖音 `parse_video` 三个域名**串行尝试** → 竞速/降超时 | `parsers/douyin/__init__.py` | 首字节 | ⭐⭐⭐⭐ | 低 |
| 4 | B站 `qn` 画质回退**最多 5 次串行 API** | `parsers/bilibili/__init__.py` | 首字节 | ⭐⭐⭐ | 低 |
| 5 | B站 video×audio×CDN **三重嵌套串行重试** | `parsers/bilibili/__init__.py` | 下载延迟 | ⭐⭐⭐ | 低 |
| 6 | 下载 `chunk_size` 256KB → 1MB | `core/download.py` | 下载吞吐 | ⭐⭐ | 极低 |
| 7 | `http_get` 重试 3+3 次、串行 sleep | `parsers/base.py` | 失败路径延迟 | ⭐⭐ | 低 |
| 8 | 提高默认并发 `max_concurrent_downloads` | `core/download.py` | 多图/多 P | ⭐⭐ | 低 |
| 9 | 进度条 `tqdm` 每 chunk update | `core/download.py` | 微优化 | ⭐ | 极低 |

---

## 二、下载 / 合并环节

### 1. 大视频走单连接下载 —— 最大的吞吐瓶颈 ⭐⭐⭐⭐⭐

**现状**：`streamd → _download_generic → _save_response_to_file` 全程是**单 TCP 连接、顺序读 chunk**。一个 90MB 上限的 B站/抖音视频，速度完全受单连接带宽和远端 CDN 限速制约。

**建议**：对支持 `Accept-Ranges: bytes` 的 CDN（B站 bilivideo、抖音 douyinvod 基本都支持）改用**分段并发下载**：
- 先发 `HEAD` 或带 `Range: bytes=0-0` 的探测请求，拿到 `Content-Length` 和是否支持 Range；
- 把文件切成 N 段（例如 4~8 段），用已有的 `curl_cffi` AsyncSession 并发 `Range` 拉取，写到同一文件的不同 offset；
- 不支持 Range 时自动回退当前单连接逻辑。

**收益**：在 CDN 对单连接限速的常见场景下，整段视频下载耗时可降 **2~4 倍**，这是「下载速度」体感最直接的一项。

**风险**：中。需要处理分段失败重试、offset 写入、`max_size_mb` 限额改成对总和判断。建议单独开关 `performance.segmented_download` 控制，默认开。

---

### 2. 抖音图集：每张图都新建 / 销毁一个 curl session ⭐⭐⭐⭐

**现状**（`download.py` `_download_douyin_image`）：

```python
async with AsyncSession(impersonate=..., http_version=V1_1, ...) as temp_session:
    response = await temp_session.get(url, stream=True)
```

`create_image_contents` 对图集里**每个 URL 各起一个下载任务**，每个任务都走到这里**新建一个 curl session**。一个 9 图图集 = 9 次 session 创建/TLS 握手/销毁。

**建议**：按 `impersonate` 策略各维护一个**长期复用的 session**（或一个 `dict[strategy_name → AsyncSession]`），命中策略后直接复用，不再 `async with` 每次新建。退出时统一 `close`。

**收益**：抖音图集解析的下载阶段省掉 N-1 次 TLS 握手，多图作品体感明显变快。

**风险**：低。只是把临时 session 提升为成员变量，注意 `terminate` 时关闭。

---

### 3. B站 video × audio × CDN 三重嵌套串行重试 ⭐⭐⭐

**现状**（`parsers/bilibili/__init__.py` `download_video_task`）：对每个清晰度 `qid`，遍历 `v_urls`（含 backup CDN）× `a_candidates`，**串行**逐个 `download_av_and_merge`。任何一个组合失败才换下一个。第一个 CDN 慢/超时时会顺序空耗。

**建议**：
- 主路径只取「最优 video URL + 最优 audio URL」直接下；
- backup CDN 仅在主组合**确实失败**后再尝试，而不是固定多层 for 嵌套；
- 可选：video 和 audio 已经是 `asyncio.gather` 并发（✅ 这点已经做对了），保持。

**收益**：正常情况下减少无谓的串行尝试；CDN 异常时更快切换。

**风险**：低。

---

### 4. 下载 chunk size 256KB → 1MB ⭐⭐

**现状**：`_save_response_to_file` / `_download_with_aiohttp` 都用 `chunk_size = 256 * 1024`。大文件下每 256KB 一次 `await write` + 一次 `bar.update`，syscall / 协程切换偏多。

**建议**：大媒体用 `1MB`（1024*1024）。配合第 9 条减少进度条刷新。

**收益**：吞吐小幅提升、CPU 占用下降，几乎零风险。

---

### 5. ffmpeg 合并 ⭐（已较优，仅记录）

`utils.merge_av` 已用 `-c copy` 流复制，是最快的合并方式 ✅。可考虑：
- 当 B站返回 `durl`（单文件渐进流，无需音视频分离）时**直接下单文件、跳过合并**——`_build_stream_ladders` 已经能返回 durl 分支，确保这条路径不会再触发合并；
- 合并命令可加 `-movflags +faststart`（利于发送端快速读取首帧，非速度项，可选）。

---

## 三、首字节 / 响应延迟环节（影响"多久开始下载"）

### 6. 抖音三域名串行尝试，每个 20s 超时 ⭐⭐⭐⭐

**现状**（`parsers/douyin/__init__.py` `_parse_douyin`）：

```python
for url in (m_douyin, iesdouyin, www_douyin):
    try: return await self.parse_video(url, vid)
    except: last_err = e
# 全失败再 ytdlp
```

`parse_video` 里 `http_get(timeout=20)`。最坏情况：m 超时(20s) → ies 超时(20s) → www 超时(20s) → 再 ytdlp，**首字节可能拖到 60s+**。

**建议**（二选一）：
- **竞速**：用 `asyncio.gather` 或「首个成功即取消其余」的方式并发请三个域名，谁先成功用谁；
- 或至少把单域名 `timeout` 从 20 降到 8~10，并保持已做对的「m/ies 优先、www 兜底」顺序。

**收益**：抖音正常作品首字节延迟显著下降；异常作品不再线性累加超时。

**风险**：低（竞速会多发 1~2 个请求，但抖音分享域名负载可接受）。

---

### 7. B站 qn 画质回退最多 5 次串行 API ⭐⭐⭐

**现状**（`_get_stream_ladders_with_qn_fallback`）：`qn_order = [None, 64, 32, 16, 6]` **串行**请求 `get_download_url`。若默认就返回 ≤720P，1 次即返回（常见✅）；但当账号/视频默认只给高清时，会**顺序打 5 次 API**才拿到可用流。

**建议**：
- 默认请求(`None`)拿到流就返回（现状已是）；
- 未命中时**直接用 `qn=64` 这一档**重试即可（720P 是体积上限内最常见的目标档），不必逐级 64→32→16→6 全跑；
- 真要逐级，可对剩余档位做有限并发而非纯串行。

**收益**：B站「未命中默认画质」场景的首字节延迟从最多 5 个 RTT 降到 2 个。

**风险**：低。

---

### 8. `http_get` 重试 3（curl）+ 3（aiohttp），带串行 sleep ⭐⭐

**现状**（`parsers/base.py`）：curl 失败重试 3 次（`sleep 0.25*(i+1)`），再整体切 aiohttp 重试 3 次（`sleep 0.35*(i+1)`）。失败路径最坏要等 6 次请求 + 多段 sleep 才抛错。

**建议**：把「curl 重试次数 + aiohttp 重试次数 + 各档超时」做成 `performance` 配置项；对**解析类**轻请求（拿 JSON / 重定向）用更短超时和更少重试，对**下载类**保留较高容错。当前一刀切偏保守。

**收益**：失败/慢站点场景下更快失败或更快回退，不阻塞后续匹配项。

**风险**：低，但要避免把容错砍太狠导致成功率下降——建议配置化、默认温和。

---

### 9. 默认下载并发 `max_concurrent_downloads = 5` ⭐⭐

`Downloader.sem = Semaphore(5)`。对多图图集 / B站多 P，超过 5 个的部分要排队。视用户带宽可调高（如 8~10）。已是配置项 `performance.max_concurrent_downloads`，建议**默认值上调**或在文档里提示。

---

## 四、已经做得不错、无需动的点 ✅

- 视频/音频并发下载 (`download_av_and_merge` 用 `asyncio.gather`)；
- B站 `video_info` / `playurl` 带 TTL 缓存，避免重复 API；
- 已下载文件按 URL hash 命名 + 存在性检查，天然去重、避免重复下载；
- 评论区抓取改为**后台任务**，与主视频发送并行，不再阻塞主流程（`main.py` `process_comment_content`）；
- 头像/封面默认不下载，保持轻量；
- ffmpeg 合并用 `-c copy` 流复制；
- B站短链优先 `HEAD` 跟随跳转，避免拉正文。

---

## 五、建议的落地顺序

1. **低风险高收益先做**：#2 抖音图集复用 session、#6 抖音域名竞速/降超时、#7 B站 qn 回退简化、#4 chunk 调大。
2. **中等**：#5 B站重试结构精简、#8 重试/超时配置化、#8 并发默认值。
3. **重头戏**：#1 分段并发下载（独立开关、单独测试），这是「下载速度」体感提升最大的一项。

> 需要的话，我可以按这个顺序逐项落地实现，并在每步给出 diff。
